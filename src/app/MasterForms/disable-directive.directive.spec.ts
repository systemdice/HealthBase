import { DisableDirectiveDirective } from './disable-directive.directive';

describe('DisableDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
